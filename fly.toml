app = "lostandfound"
primary_region = "hkg"

[build]
  dockerfile = "Dockerfile"

[mounts]
  source = "lf_data"
  destination = "/app"

[[services]]
  internal_port = 5000
  force_https = false
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
